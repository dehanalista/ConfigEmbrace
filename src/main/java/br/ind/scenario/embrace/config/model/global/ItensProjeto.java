package br.ind.scenario.embrace.config.model.global;

public class ItensProjeto {

    private Projeto projeto;

    /*
    TItensProjeto = class(TNode, INode, IItemTreeView, IPrototypeItensProjeto)
    protected
    constructor Create(AProjeto: TProjeto; ANome: string; AId: Integer; ATipoClasse: ETipoClasse); overload;
    constructor Create(AXml: IXMLNode; AProjeto: TProjeto; ATipoClasse: ETipoClasse); overload;

    procedure SetProjeto(AProjeto: TProjeto);
    function Clone(var AItemProjeto: TItensProjeto; AProjeto: TProjeto): TItensProjeto; overload;
    function GetProjeto: TProjeto;
    public
    function Clone(AProjeto: TProjeto; AId: Integer): TItensProjeto; overload; virtual; abstract;
    function SalvarXML(AXml: IXMLNode): IXMLNode; override;

    property Projeto: TProjeto read GetProjeto write SetProjeto;
    end;

     */
}
